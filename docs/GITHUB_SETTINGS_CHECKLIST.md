# GitHub Settings Checklist

- Set default branch = `dev`
- Protect `main`: require PR, 1 approval, require CI
- Protect `dev`: require PR (optional 0-1 approvals), require CI
- Disallow force pushes
- Auto-delete branches after merge (optional)
